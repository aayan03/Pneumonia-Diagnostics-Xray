"""
Flask app — legacy reference only. NOT used for running the application.

The active entry point is:  streamlit run streamlit_app.py
"""
# This file is intentionally left as a stub so git history is preserved.
# All application logic has moved to streamlit_app.py.
